apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: taskfarm-external-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: taskfarm-secret-store
    kind: SecretStore
  target:
    name: taskfarm-secret
    creationPolicy: Owner
  data:
    - secretKey: DB_USERNAME
      remoteRef:
        key: /taskfarm/prod/db/username

    - secretKey: DB_PASSWORD
      remoteRef:
        key: /taskfarm/prod/db/password

    - secretKey: JWT_SECRET
      remoteRef:
        key: /taskfarm/prod/jwt/secret

    - secretKey: REDIS_USERNAME
      remoteRef:
        key: /taskfarm/prod/redis/username

    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: /taskfarm/prod/redis/password
