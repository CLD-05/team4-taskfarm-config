apiVersion: v1
kind: ServiceAccount
metadata:
  name: taskfarm-admin-sa
  labels:
    app: taskfarm-admin
  # [MFA] IRSA Role ARN 어노테이션은 환경별로 다름 →
  #       base에 박지 않고 dev/prod overlay의 patch로 주입