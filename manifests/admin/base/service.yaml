apiVersion: v1
kind: Service
metadata:
  name: taskfarm-admin
  labels:
    app: taskfarm-admin
spec:
  type: ClusterIP
  selector:
    app: taskfarm-admin
  ports:
    - name: http
      port: 80
      targetPort: 8081
      protocol: TCP
