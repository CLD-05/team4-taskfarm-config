# =====================================================================
# [담당2] admin 앱 - base ConfigMap
# =====================================================================
# 목표: admin 비밀 아닌 환경변수. user configmap과 거의 동일.
# ⚠️ 차이: admin은 AI 워커 안 돌림 → SPRING_PROFILES_ACTIVE에 worker 불필요 (admin yml 확인)
#         DB는 user와 같은 RDS 공유 (DB_NAME 동일)
# 작성할 것: user/base/configmap.yaml 참고 (비밀은 여기 X)
# =====================================================================

# TODO: 여기에 admin ConfigMap 작성
