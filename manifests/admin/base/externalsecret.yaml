# =====================================================================
# [담당2] admin 앱 - ExternalSecret (ESO)
# =====================================================================
# 목표: admin 비밀(JWT_SECRET, DB_USERNAME/PASSWORD) → K8s Secret.
#       user의 externalsecret.yaml과 동일 패턴 (담당3이 정한 SecretStore 사용).
# ⚠️ admin이 gemini 호출 안 하면 GEMINI_API_KEY 불필요 (확인)
# 작성할 것: user/base/externalsecret.yaml 참고, target.name=taskfarm-admin-secret
# =====================================================================

# TODO: 여기에 admin ExternalSecret 작성
