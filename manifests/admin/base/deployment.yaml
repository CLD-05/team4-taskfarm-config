apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskfarm-admin
  labels:
    app: taskfarm-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taskfarm-admin
  template:
    metadata:
      labels:
        app: taskfarm-admin
    spec:
      serviceAccountName: taskfarm-admin-sa
      containers:
        - name: taskfarm-admin
          image: taskfarm-admin-img
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: taskfarm-admin-config
            - configMapRef:
                name: taskfarm-endpoints
          env:
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: taskfarm-admin-secret
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: taskfarm-admin-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: taskfarm-admin-secret
                  key: JWT_SECRET
          # ⚠️ admin은 context-path /admin → actuator 경로도 /admin 접두사
          readinessProbe:
            httpGet:
              path: /admin/actuator/health/readiness
              port: 8081
            initialDelaySeconds: 120
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /admin/actuator/health/liveness
              port: 8081
            initialDelaySeconds: 240
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 10
          resources:
            requests:
              cpu: 1000m
              memory: 2Gi
            limits:
              cpu: 1000m
              memory: 2Gi
