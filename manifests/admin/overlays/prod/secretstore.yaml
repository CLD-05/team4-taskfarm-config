apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: taskfarm-admin-secret-store
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-northeast-2
      auth:
        jwt:
          serviceAccountRef:
            name: taskfarm-admin-sa
