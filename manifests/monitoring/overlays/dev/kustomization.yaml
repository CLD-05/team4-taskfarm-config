apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: taskfarm-dev

resources:
  - ../../base

patches:
  - path: hpa-patch.yaml
    target:
      kind: HorizontalPodAutoscaler
      name: taskfarm-user
