apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# ⚠️ ingress는 여기 없음 — prod overlay에서만 추가 (dev=port-forward)
resources:
  - serviceaccount.yaml
  - secretstore.yaml
  - externalsecret.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
commonLabels:
  app.kubernetes.io/part-of: taskfarm
  app.kubernetes.io/component: user
