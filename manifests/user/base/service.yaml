apiVersion: v1
kind: Service
metadata:
  name: taskfarm-user
  labels:
    app: taskfarm-user
spec:
  type: ClusterIP
  selector:
    app: taskfarm-user
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP

