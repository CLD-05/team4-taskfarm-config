apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: taskfarm-user-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: taskfarm-secret-store
    kind: SecretStore
  target:
    name: taskfarm-user-secret
    creationPolicy: Owner
  data:
    # ⚠️ prod 경로 (dev 아님!)
    - secretKey: DB_USERNAME
      remoteRef: { key: team4/taskfarm/prod/db, property: username }
    - secretKey: DB_PASSWORD
      remoteRef: { key: team4/taskfarm/prod/db, property: password }
    - secretKey: JWT_SECRET
      remoteRef: { key: team4/taskfarm/prod/jwt-secret }
    - secretKey: GEMINI_API_KEY
      remoteRef: { key: team4/taskfarm/prod/gemini-api-key }
